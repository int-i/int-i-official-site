const USERS = "/users";
const JOIN = "/join";
const LOGIN = "/login";
const LOGOUT = "/logout";


const API = "/api";
// const GLOBAL_SEARCH = "";


const routes = {
	users: USERS,
	login: LOGIN,
	logout: LOGOUT,
    join: JOIN,
    api: API
}

export default routes;